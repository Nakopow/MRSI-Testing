# ── Web framework ─────────────────────────────────────────────────────────────
flask==3.0.0
gunicorn>=21.2.0

# ── Authentication & OAuth ────────────────────────────────────────────────────
authlib>=1.3.0           # Google OAuth 2.0 (OpenID Connect)

# ── Database (user accounts & preferences) ───────────────────────────────────
flask-sqlalchemy>=3.1.0  # ORM — SQLite locally, PostgreSQL (Supabase) in prod
psycopg2-binary>=2.9.0   # PostgreSQL driver required for Supabase

# ── Web scraping & parsing ────────────────────────────────────────────────────
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0
feedparser>=6.0.0
certifi>=2024.0.0

# ── AI / LLM ──────────────────────────────────────────────────────────────────
google-generativeai>=0.8.0

# ── Document generation ───────────────────────────────────────────────────────
python-docx>=1.1.0

# ── Config & environment ──────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Scheduling (Railway / long-running deployments) ──────────────────────────
APScheduler>=3.10.0
pytz>=2023.3

# ── Cloud storage (Vercel / production) ──────────────────────────────────────
supabase>=2.0.0          # Supabase Storage for artifacts + PostgreSQL for users
boto3>=1.26.0            # AWS S3 alternative
